/** @type {import('next').NextConfig} */
const nextConfig = {
  // Opcional: Se as imagens dos pratos estiverem em um domínio externo,
  // você pode configurá-lo aqui. Por exemplo:
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'firebasestorage.googleapis.com',
  //     },
  //   ],
  // },
};

export default nextConfig;